# Experimental pyQt interface for excel manipulation and visualization
To debug this project, first git clone to local. Then install the necessary python packages:
```shell
pip install PyQt5 pandas openpyxl
```
